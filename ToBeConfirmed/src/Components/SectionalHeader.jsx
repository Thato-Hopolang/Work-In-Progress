/* eslint-disable react/prop-types */

const SectionalHeader = ({ header, paragraph}) => {
  return (
    <div>
        <h1>{header}</h1>
        <p>{paragraph}</p>
    </div>
  )
}

export default SectionalHeader